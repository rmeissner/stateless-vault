{
  "name": "stateless-vault",
  "description": "A 'stateless' multisignature smart wallet for Ethereum",
  "license": "LGPL-3.0-only",
  "private": true,
  "scripts": {
    "build": "yarn && yarn workspaces run build",
    "test": "yarn build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
